# VetrIAge RAG System Dependencies
# Install with: pip install -r requirements.txt
# Or with uv: uv pip install -r requirements.txt

# Core dependencies
biopython>=1.83           # PubMed API access via Entrez
openai>=1.12.0           # OpenAI embeddings and API
anthropic>=0.21.0        # Claude LLM for diagnosis
numpy>=1.24.0            # Numerical operations
requests>=2.31.0         # HTTP requests
python-dotenv>=1.0.0     # Environment variables

# Vector search (choose one)
faiss-cpu>=1.7.4         # FAISS vector search (CPU version) - for development
# faiss-gpu>=1.7.4       # FAISS GPU version (faster, requires CUDA) - uncomment for production
# pgvector>=0.2.0        # PostgreSQL vector extension - uncomment for production with PostgreSQL

# Optional but recommended
langchain>=0.1.9         # RAG orchestration framework
tenacity>=8.2.3          # Retry logic with exponential backoff
pydantic>=2.0.0          # Data validation
tqdm>=4.66.0             # Progress bars
scikit-learn>=1.3.0      # ML utilities and metrics

# Development/Testing
pytest>=7.4.0            # Testing framework
pytest-asyncio>=0.21.0   # Async testing
pytest-cov>=4.1.0        # Code coverage
black>=23.0.0            # Code formatting
mypy>=1.5.0              # Type checking
